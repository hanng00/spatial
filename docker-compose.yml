
services:
  ingestion:
    build:
      context: .
      dockerfile: ingestion/Dockerfile
    image: spatial/ingestion:latest
    networks:
      - spatial-network
    # No ports needed - accessed via Docker network
    # Environment variables passed at runtime via ContainerExecutor

  transformations_dbt:
    build:
      context: .
      dockerfile: transformations_dbt/Dockerfile
    image: spatial/transformations_dbt:latest
    networks:
      - spatial-network
    # Environment variables passed at runtime

  # cognition:
  #   build:
  #     context: .
  #     dockerfile: cognition/Dockerfile
  #   image: spatial/cognition:latest
  #   networks:
  #     - spatial-network
  #   # Environment variables passed at runtime

networks:
  spatial-network:
    driver: bridge
    name: spatial-network

